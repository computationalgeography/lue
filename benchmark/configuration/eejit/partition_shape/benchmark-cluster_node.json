{
    "scenario": "cluster_node",
    "count": 1,
    "worker": {
        "type": "cluster_node",
        "pool": {
            # Half the size of the max nr_workers used in scaling experiment
            "size": 4
        }
    }
}
