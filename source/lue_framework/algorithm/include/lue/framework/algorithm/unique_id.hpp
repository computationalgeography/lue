#pragma once
// #include "lue/framework/core/array.hpp"


namespace lue {

template<
    typename PartitionedArray
>
PartitionedArray& unique_id(
    PartitionedArray& array)
{
    // PartitionedArray result{shape(array)};

    // Attach unique_id to all partitions
    // Per partition an increasing number


    return array;
}

}  // namespace lue
