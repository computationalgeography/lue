# add_subdirectory(airpolution)
if(LUE_FRAMEWORK_WITH_BENCHMARKS)
    # add_subdirectory(heat_ring)
    # add_subdirectory(routing)
    # add_subdirectory(wildfire)
endif()
# add_subdirectory(land_use)
