name: Windows Conda package

on: [push]

jobs:
    build:

        strategy:
            matrix:
                os: ["windows-2019"]
                python-version: [3.9]

        runs-on: ${{ matrix.os }}

        name: ${{ matrix.os }} - ${{ matrix.python-version }}

        steps:

        - name: checkout lue
          uses: actions/checkout@v2
